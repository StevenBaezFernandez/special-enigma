version: '3.8'

services:
  # UNIFIED GATEWAY (NGINX)
  nginx:
    # Pinning to semantic version + SHA256 digest for immutability
    image: nginx:1.27-alpine@sha256:f46cb72c7df02710e693e863a983ac42f6a9579058a59a35f1ae36c9958e4ce0
    container_name: virteex-unified-gateway
    ports:
      - "3000:80"
      - "3443:443"
    volumes:
      - ./platform/infrastructure/docker/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./platform/infrastructure/docker/nginx/certs:/etc/nginx/certs
    depends_on:
      - api-gateway
      - identity-service
      - inventory-service
      - billing-service
      - crm-service
      - fiscal-service
      - scheduler-service
      - gateway
      - catalog-service

  # API GATEWAY (Monolith)
  api-gateway:
    image: node:22.12.0-alpine
    container_name: virteex-api-gateway
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - VIRTEEX_HMAC_SECRET=${VIRTEEX_HMAC_SECRET}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - POSTGRES_URL=${POSTGRES_URL:-postgres://virteex:${POSTGRES_PASSWORD}@postgres:5432/virteex_erp}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=virteex
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_NAME=virteex_erp
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-api-gateway
    depends_on:
      - postgres
      - redis
      - kafka

  # IDENTITY SERVICE
  identity-service:
    image: node:22.12.0-alpine
    container_name: virteex-identity-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - IDENTITY_DB_HOST=postgres
      - IDENTITY_DB_PORT=5432
      - IDENTITY_DB_USER=virteex
      - IDENTITY_DB_PASSWORD=${POSTGRES_PASSWORD}
      - IDENTITY_DB_NAME=virteex_identity
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-identity-service
    depends_on:
      - postgres
      - redis
      - kafka

  # INVENTORY SERVICE
  inventory-service:
    image: node:22.12.0-alpine
    container_name: virteex-inventory-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - INVENTORY_DB_HOST=postgres
      - INVENTORY_DB_PORT=5432
      - INVENTORY_DB_USER=virteex
      - INVENTORY_DB_PASSWORD=${POSTGRES_PASSWORD}
      - INVENTORY_DB_NAME=virteex_inventory
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-inventory-service
    depends_on:
      - postgres
      - redis
      - kafka

  # BILLING SERVICE
  billing-service:
    image: node:22.12.0-alpine
    container_name: virteex-billing-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - BILLING_DB_HOST=postgres
      - BILLING_DB_PORT=5432
      - BILLING_DB_USER=virteex
      - BILLING_DB_PASSWORD=${POSTGRES_PASSWORD}
      - BILLING_DB_NAME=virteex_billing
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-billing-service
    depends_on:
      - postgres
      - redis
      - kafka

  # CRM SERVICE
  crm-service:
    image: node:22.12.0-alpine
    container_name: virteex-crm-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - CRM_DB_HOST=postgres
      - CRM_DB_PORT=5432
      - CRM_DB_USER=virteex
      - CRM_DB_PASSWORD=${POSTGRES_PASSWORD}
      - CRM_DB_NAME=virteex_crm
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-crm-service
    depends_on:
      - postgres
      - redis
      - kafka

  # FISCAL SERVICE
  fiscal-service:
    image: node:22.12.0-alpine
    container_name: virteex-fiscal-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - FISCAL_DB_HOST=postgres
      - FISCAL_DB_PORT=5432
      - FISCAL_DB_USER=virteex
      - FISCAL_DB_PASSWORD=${POSTGRES_PASSWORD}
      - FISCAL_DB_NAME=virteex_fiscal
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-fiscal-service
    depends_on:
      - postgres
      - redis
      - kafka

  # SCHEDULER SERVICE
  scheduler-service:
    image: node:22.12.0-alpine
    container_name: virteex-scheduler-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-scheduler-service
    depends_on:
      - kafka

  # ACCOUNTING SERVICE
  accounting-service:
    image: node:22.12.0-alpine
    container_name: virteex-accounting-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - ACCOUNTING_DB_HOST=postgres
      - ACCOUNTING_DB_PORT=5432
      - ACCOUNTING_DB_USER=virteex
      - ACCOUNTING_DB_PASSWORD=${POSTGRES_PASSWORD}
      - ACCOUNTING_DB_NAME=virteex_accounting
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-accounting-service
    depends_on:
      - postgres
      - redis
      - kafka

  # PAYROLL SERVICE
  payroll-service:
    image: node:22.12.0-alpine
    container_name: virteex-payroll-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - PAYROLL_DB_HOST=postgres
      - PAYROLL_DB_PORT=5432
      - PAYROLL_DB_USER=virteex
      - PAYROLL_DB_PASSWORD=${POSTGRES_PASSWORD}
      - PAYROLL_DB_NAME=virteex_payroll
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-payroll-service
    depends_on:
      - postgres
      - redis
      - kafka

  # PROJECTS SERVICE
  projects-service:
    image: node:22.12.0-alpine
    container_name: virteex-projects-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - PROJECTS_DB_HOST=postgres
      - PROJECTS_DB_PORT=5432
      - PROJECTS_DB_USER=virteex
      - PROJECTS_DB_PASSWORD=${POSTGRES_PASSWORD}
      - PROJECTS_DB_NAME=virteex_projects
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-projects-service
    depends_on:
      - postgres
      - redis
      - kafka

  # MANUFACTURING SERVICE
  manufacturing-service:
    image: node:22.12.0-alpine
    container_name: virteex-manufacturing-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - MANUFACTURING_DB_HOST=postgres
      - MANUFACTURING_DB_PORT=5432
      - MANUFACTURING_DB_USER=virteex
      - MANUFACTURING_DB_PASSWORD=${POSTGRES_PASSWORD}
      - MANUFACTURING_DB_NAME=virteex_manufacturing
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-manufacturing-service
    depends_on:
      - postgres
      - redis
      - kafka

  # TREASURY SERVICE
  treasury-service:
    image: node:22.12.0-alpine
    container_name: virteex-treasury-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - TREASURY_DB_HOST=postgres
      - TREASURY_DB_PORT=5432
      - TREASURY_DB_USER=virteex
      - TREASURY_DB_PASSWORD=${POSTGRES_PASSWORD}
      - TREASURY_DB_NAME=virteex_treasury
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-treasury-service
    depends_on:
      - postgres
      - redis
      - kafka

  # PURCHASING SERVICE
  purchasing-service:
    image: node:22.12.0-alpine
    container_name: virteex-purchasing-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - PURCHASING_DB_HOST=postgres
      - PURCHASING_DB_PORT=5432
      - PURCHASING_DB_USER=virteex
      - PURCHASING_DB_PASSWORD=${POSTGRES_PASSWORD}
      - PURCHASING_DB_NAME=virteex_purchasing
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-purchasing-service
    depends_on:
      - postgres
      - redis
      - kafka

  # BI SERVICE
  bi-service:
    image: node:22.12.0-alpine
    container_name: virteex-bi-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - BI_DB_HOST=postgres
      - BI_DB_PORT=5432
      - BI_DB_USER=virteex
      - BI_DB_PASSWORD=${POSTGRES_PASSWORD}
      - BI_DB_NAME=virteex_bi
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-bi-service
    depends_on:
      - postgres
      - redis
      - kafka

  # ADMIN SERVICE
  admin-service:
    image: node:22.12.0-alpine
    container_name: virteex-admin-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - ADMIN_DB_HOST=postgres
      - ADMIN_DB_PORT=5432
      - ADMIN_DB_USER=virteex
      - ADMIN_DB_PASSWORD=${POSTGRES_PASSWORD}
      - ADMIN_DB_NAME=virteex_admin
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-admin-service
    depends_on:
      - postgres
      - redis
      - kafka

  # FIXED ASSETS SERVICE
  fixed-assets-service:
    image: node:22.12.0-alpine
    container_name: virteex-fixed-assets-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - FIXED_ASSETS_DB_HOST=postgres
      - FIXED_ASSETS_DB_PORT=5432
      - FIXED_ASSETS_DB_USER=virteex
      - FIXED_ASSETS_DB_PASSWORD=${POSTGRES_PASSWORD}
      - FIXED_ASSETS_DB_NAME=virteex_fixed_assets
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-fixed-assets-service
    depends_on:
      - postgres
      - redis
      - kafka

  # CATALOG SERVICE (Subgraph)
  catalog-service:
    image: node:22.12.0-alpine
    container_name: virteex-catalog-service
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - CATALOG_DB_HOST=postgres
      - CATALOG_DB_PORT=5432
      - CATALOG_DB_USER=virteex
      - CATALOG_DB_PASSWORD=${POSTGRES_PASSWORD}
      - CATALOG_DB_NAME=virteex_catalog
      - DB_DRIVER=postgres
      - NODE_ENV=${NODE_ENV:-development}
      - KAFKA_BROKERS=${KAFKA_BROKERS:-kafka:9092}
    command: npx nx serve virteex-catalog-service
    depends_on:
      - postgres
      - kafka

  # GATEWAY (Apollo)
  gateway:
    image: node:22.12.0-alpine
    container_name: virteex-gateway
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PORT=3000
      - CATALOG_SERVICE_URL=http://catalog-service:3000/graphql
      - NODE_ENV=${NODE_ENV:-development}
    command: npx nx serve virteex-gateway
    depends_on:
      - catalog-service

  postgres:
    # Pinning to semantic version + SHA256 digest for immutability
    image: timescale/timescaledb:2.13.0-pg15@sha256:a4994627d9067ec44b1da8483b3de795389b3a783a9c8db915c7b2ca5b172f52
    container_name: virteex-postgres
    restart: always
    environment:
      POSTGRES_USER: virteex
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: virteex_erp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./platform/infrastructure/docker/init-scripts/01-create-databases.sh:/docker-entrypoint-initdb.d/01-create-databases.sh

  redis:
    # Pinning to semantic version + SHA256 digest for immutability
    image: redis:7.2.4-alpine@sha256:8b81dd37ff027bec4e516d41acfbe9fe2460070dc6d4a4570a2ac5b9d59df065
    container_name: virteex-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  zookeeper:
    # Pinning to semantic version + SHA256 digest for immutability
    image: bitnami/zookeeper:3.9.1@sha256:f56ea4f9013021b45597fb2133efa1a64218feeb8866cf2a998be1c58ac021b4
    container_name: virteex-zookeeper
    ports:
      - "2181:2181"
    environment:
      # Hardened: Disallow anonymous login
      - ALLOW_ANONYMOUS_LOGIN=no

  kafka:
    # Pinning to semantic version + SHA256 digest for immutability
    image: bitnami/kafka:3.6.1@sha256:b7bccfd8bb24e342fa0465e4dbcf9334236fc4df5483bc30edbb585071a2f0b0
    container_name: virteex-kafka
    ports:
      - "9092:9092"
    environment:
      - KAFKA_BROKER_ID=1
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      # Hardened: Use SASL_PLAINTEXT for internal and external listeners
      - KAFKA_CFG_LISTENERS=SASL_PLAINTEXT://:9092
      - KAFKA_CFG_ADVERTISED_LISTENERS=SASL_PLAINTEXT://localhost:9092
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=SASL_PLAINTEXT:SASL_PLAINTEXT
      - ALLOW_PLAINTEXT_LISTENER=no
    depends_on:
      - zookeeper

  # Observability Stack
  jaeger:
    # Pinning to semantic version + SHA256 digest for immutability
    image: jaegertracing/all-in-one:1.53@sha256:ab6f1a1f0fb49ea08bcd19f6b84f6081d0d44b364b6de148e1798eb5816bacac
    container_name: virteex-jaeger
    ports:
      - "16686:16686"
      - "4317:4317"
      - "4318:4318"

  prometheus:
    # Pinning to semantic version + SHA256 digest for immutability
    image: prom/prometheus:v2.49.1@sha256:d1010a52641b282b6c8584431b7e01589b4d556396904396446f72db54549aa9
    container_name: virteex-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./platform/observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    # Pinning to semantic version + SHA256 digest for immutability
    image: grafana/grafana:10.3.3@sha256:e932bd6ed0e026595b08483cd0141e5103e1ab7ff8604839ff899b8dc54cabcb
    container_name: virteex-grafana
    ports:
      - "3001:3000"
    depends_on:
      - prometheus

volumes:
  postgres_data:
  redis_data:
# This file defines the full local development environment with extracted microservices.
