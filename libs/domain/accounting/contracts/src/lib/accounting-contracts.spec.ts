import { accountingContracts } from './accounting-contracts';

describe('accountingContracts', () => {
  it('should work', () => {
    expect(accountingContracts()).toEqual('accounting-contracts');
  });
});
