import { manufacturingApplication } from './manufacturing-application';

describe('manufacturingApplication', () => {
  it('should work', () => {
    expect(manufacturingApplication()).toEqual('manufacturing-application');
  })
})
