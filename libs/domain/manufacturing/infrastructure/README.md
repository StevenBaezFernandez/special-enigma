# manufacturing-infrastructure

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build manufacturing-infrastructure` to build the library.

## Running unit tests

Run `nx test manufacturing-infrastructure` to execute the unit tests via [Jest](https://jestjs.io).
