import { manufacturingDomain } from './manufacturing-domain';

describe('manufacturingDomain', () => {
  it('should work', () => {
    expect(manufacturingDomain()).toEqual('manufacturing-domain');
  })
})
