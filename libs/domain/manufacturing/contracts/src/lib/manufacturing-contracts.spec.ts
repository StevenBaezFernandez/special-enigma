import { manufacturingContracts } from './manufacturing-contracts';

describe('manufacturingContracts', () => {
  it('should work', () => {
    expect(manufacturingContracts()).toEqual('manufacturing-contracts');
  })
})
