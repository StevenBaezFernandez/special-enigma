# fixed-assets-infrastructure

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fixed-assets-infrastructure` to build the library.

## Running unit tests

Run `nx test fixed-assets-infrastructure` to execute the unit tests via [Jest](https://jestjs.io).
