import { fixedAssetsPresentation } from './fixed-assets-presentation';

describe('fixedAssetsPresentation', () => {
  it('should work', () => {
    expect(fixedAssetsPresentation()).toEqual('fixed-assets-presentation');
  })
})
