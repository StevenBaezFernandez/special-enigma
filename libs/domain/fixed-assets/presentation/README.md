# fixed-assets-presentation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fixed-assets-presentation` to build the library.

## Running unit tests

Run `nx test fixed-assets-presentation` to execute the unit tests via [Jest](https://jestjs.io).
