import { fixedAssetsContracts } from './fixed-assets-contracts';

describe('fixedAssetsContracts', () => {
  it('should work', () => {
    expect(fixedAssetsContracts()).toEqual('fixed-assets-contracts');
  })
})
