# fixed-assets-application

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fixed-assets-application` to build the library.

## Running unit tests

Run `nx test fixed-assets-application` to execute the unit tests via [Jest](https://jestjs.io).
