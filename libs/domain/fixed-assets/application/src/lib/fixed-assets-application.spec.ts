import { fixedAssetsApplication } from './fixed-assets-application';

describe('fixedAssetsApplication', () => {
  it('should work', () => {
    expect(fixedAssetsApplication()).toEqual('fixed-assets-application');
  })
})
