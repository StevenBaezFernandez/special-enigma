# fiscal-infrastructure

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fiscal-infrastructure` to build the library.

## Running unit tests

Run `nx test fiscal-infrastructure` to execute the unit tests via [Jest](https://jestjs.io).
