import { fiscalPresentation } from './fiscal-presentation';

describe('fiscalPresentation', () => {
  it('should work', () => {
    expect(fiscalPresentation()).toEqual('fiscal-presentation');
  })
})
