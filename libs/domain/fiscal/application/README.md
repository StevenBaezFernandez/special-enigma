# fiscal-application

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fiscal-application` to build the library.

## Running unit tests

Run `nx test fiscal-application` to execute the unit tests via [Jest](https://jestjs.io).
