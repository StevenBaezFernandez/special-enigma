# admin-presentation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-presentation` to build the library.

## Running unit tests

Run `nx test admin-presentation` to execute the unit tests via [Jest](https://jestjs.io).
