import { payrollApplication } from './payroll-application';

describe('payrollApplication', () => {
  it('should work', () => {
    expect(payrollApplication()).toEqual('payroll-application');
  })
})
