# payroll-infrastructure

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build payroll-infrastructure` to build the library.

## Running unit tests

Run `nx test payroll-infrastructure` to execute the unit tests via [Jest](https://jestjs.io).
