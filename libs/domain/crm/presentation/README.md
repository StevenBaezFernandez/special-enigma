# crm-presentation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build crm-presentation` to build the library.

## Running unit tests

Run `nx test crm-presentation` to execute the unit tests via [Jest](https://jestjs.io).
