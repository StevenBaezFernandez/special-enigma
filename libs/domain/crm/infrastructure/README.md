# crm-infrastructure

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build crm-infrastructure` to build the library.

## Running unit tests

Run `nx test crm-infrastructure` to execute the unit tests via [Jest](https://jestjs.io).
