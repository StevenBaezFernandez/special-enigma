import { projectsPresentation } from './projects-presentation';

describe('projectsPresentation', () => {
  it('should work', () => {
    expect(projectsPresentation()).toEqual('projects-presentation');
  })
})
