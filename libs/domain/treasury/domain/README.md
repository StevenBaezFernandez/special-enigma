# treasury-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build treasury-domain` to build the library.

## Running unit tests

Run `nx test treasury-domain` to execute the unit tests via [Jest](https://jestjs.io).
