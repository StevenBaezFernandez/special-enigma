# bi-application

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build bi-application` to build the library.

## Running unit tests

Run `nx test bi-application` to execute the unit tests via [Jest](https://jestjs.io).
