{
  "name": "domain-identity-domain",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domains/identity/domain/src",
  "projectType": "library",
  "prefix": "virteex",
  "tags": [
    "scope:identity",
    "type:domain",
    "platform:agnostic",
    "tenant-mode:schema",
    "criticality:high"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/libs/domains/identity/domain",
        "main": "libs/domains/identity/domain/src/index.ts",
        "tsConfig": "libs/domains/identity/domain/tsconfig.lib.json",
        "assets": [
          "libs/domains/identity/domain/*.md"
        ]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": [
        "{options.reportsDirectory}"
      ],
      "options": {
        "reportsDirectory": "../../../../coverage/libs/domains/identity/domain"
      }
    }
  }
}