# Supergraph SDL Placeholder for build-time composition
# In a real pipeline, this file would be generated by @apollo/rover or similar
schema
  @link(url: "https://specs.apollo.dev/link/v1.0")
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@extends", "@external", "@requires", "@provides"])
{
  query: Query
}

type Query {
  _service: _Service!
}

type _Service {
  sdl: String
}
