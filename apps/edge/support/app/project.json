{
  "name": "edge-support-app",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/edge/support/app/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/apps/edge/support/app",
        "main": "apps/edge/support/app/src/main.ts",
        "tsConfig": "apps/edge/support/app/tsconfig.app.json",
        "compiler": "tsc",
        "target": "node",
        "webpackConfig": "apps/edge/support/app/webpack.config.js"
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false
        },
        "development": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "edge-support-app:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "edge-support-app:build:development"
        },
        "production": {
          "buildTarget": "edge-support-app:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "apps/edge/support/app/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": [
    "scope:edge",
    "scope:support",
    "type:bff",
    "platform:node",
    "criticality:medium"
  ]
}
