# Informe de Disponibilidad Operativa - Virteex ERP (Análisis de Brechas Funcionales y Comerciales)

Este informe detalla exhaustivamente los vacíos funcionales, mocks, y puntos críticos de mejora identificados en el código fuente actual de Virteex ERP para alcanzar un estado 100% funcional y comercial (Ready for Production). El análisis cubre desde la capa de identidad hasta los módulos fiscales y de facturación.

---

## 1. Identidad y Onboarding (Critical Path)

### 1.1. Servicio de Notificaciones (Mocks vs. Realidad)
- **Estado Actual:** El `NodemailerNotificationService` en `libs/domains/identity/infrastructure` está implementado para usar `MailQueueProducer` y `MailProcessor`. Sin embargo, la configuración de `SMTP` y `REDIS` depende de variables de entorno que a menudo faltan, llevando a fallos silenciosos.
- **Acción Requerida:**
    - **Validación Estricta:** Validar estrictamente `SMTP_HOST`, `SMTP_USER`, `SMTP_PASS` al iniciar el servicio.
    - **Dead Letter Queue (DLQ):** Configurar `bullmq` para mover trabajos fallidos a una cola de `failed-emails` después de 3 intentos fallidos.
    - **Soporte Multi-Proveedor:** Abstraer `nodemailer` para permitir el uso de APIs transaccionales como SendGrid o AWS SES.

### 1.2. Flujo de Registro y Verificación (Onboarding)
- **Estado Actual:** `InitiateSignupUseCase` genera un OTP y lo guarda en caché (Redis). `VerifySignupUseCase` valida el OTP. `CompleteOnboardingUseCase` crea el usuario y la empresa.
- **Problema Crítico (Hardcoded/Mock):**
    - En `CompleteOnboardingUseCase`, se usa `console.log` en lugar de una llamada real al servicio de notificaciones.
    - Falta validación de duplicados para `taxId` en `CompanyRepository` antes de persistir, causando errores 500 en lugar de 409.
- **Acción Requerida:**
    - Reemplazar `console.log` con `await this.notificationService.sendWelcomeEmail(user);`.
    - Agregar validación de duplicados para `taxId` en `CompanyRepository`.

### 1.3. Seguridad y Sesiones
- **Estado Actual:** `AuthService` en el frontend maneja tokens JWT.
- **Mejora Necesaria:**
    - **Refresh Token Rotation:** El backend genera `refreshToken`, pero el frontend carece de un interceptor robusto para renovar el token automáticamente.
    - **MFA Enforcement:** Falta un flujo forzado de configuración de MFA (QR Code scan) en el primer login para usuarios privilegiados.

---

## 2. Motor Fiscal y Facturación (Revenue Core)

Este módulo es crítico y contiene múltiples stubs y adaptadores "Null" o "Mock".

### 2.1. Proveedores de Certificación (PAC / DIAN)
- **FinkokPacProvider (MX):**
    - **Estado:** Implementado con lógica real, pero manejo de errores básico.
    - **Mejora:** Implementar "Circuit Breaker" (e.g., `opossum`) y persistir el XML firmado antes de confirmar la transacción.
- **NullPacProvider (Fallback):**
    - **Estado:** Usado por defecto si no se configura PAC.
    - **Riesgo:** Invalidez legal de facturas en producción.
    - **Acción:** Deshabilitar en producción y forzar configuración válida.
- **DianFiscalAdapter (CO):**
    - **Estado:** Implementación incompleta (`MockFiscalProvider` es usado a menudo en lugar de una implementación real robusta, aunque `DianFiscalAdapter` existe, tiene lógica básica y depende de variables de entorno para llaves).
    - **Acción:** Completar la lógica de firmado XML-DSig (Enveloped) y consumo del WSDL de la DIAN. Eliminar el uso de claves efímeras generadas en runtime si no hay clave privada configurada.

### 2.2. Cálculo de Impuestos (Tax Engine)
- **TaxRuleEngine:**
    - **Estado:** Funcional básico.
    - **Problema:** Estrategia de impuestos de EE.UU. usa tasa fija incorrecta.
    - **Mejora:** Integrar API de cálculo de impuestos en tiempo real o tabla de tasas por Zip Code.
- **Race Conditions en Facturación:**
    - **Estado:** Riesgo de inconsistencia si falla el servidor durante el timbrado.
    - **Solución:** Implementar Idempotencia estricta y Locking Optimista o Distribuido.

### 2.3. Generación de Documentos (XML / PDF)
- **Builders:** `MxFiscalDocumentBuilder` y `CoFiscalDocumentBuilder` generan XMLs simplificados sin validación robusta.
- **Acción:** Integrar validación XSD local antes del envío al PAC.

---

## 3. Ops Dashboard & Gestión de Tenants (Soporte Interno)

La aplicación `virteex-ops` está incompleta.

### 3.1. Gestión de Tenants
- **TenantsComponent:**
    - **Estado:** Muestra tabla básica, botón "New Tenant" sin implementar.
    - **Acción:** Implementar `CreateTenantComponent` y conectarlo al backend.
- **TenantService (Mock):**
    - **Estado:** Usa datos mockeados.
    - **Mejora:** Implementar búsqueda y filtrado real en el backend.

### 3.2. Autenticación de Operadores
- **AuthService (Ops):**
    - **Estado:** Mock hardcodeado con token falso.
    - **Riesgo:** Acceso no restringido.
    - **Acción URGENTE:** Conectar al `IdentityService` real y aplicar RBAC estricto.

---

## 4. Conclusiones Generales & Fiabilidad Transversal

### 4.1. Manejo de Errores y Logging
- **Observabilidad:** Falta logging estructurado (JSON).
- **Acción:** Integrar `winston` o `pino` con formato JSON y trazas distribuidas.

### 4.2. Estrategia de Reintentos (Resiliency)
- **Estado Actual:** Falta lógica de reintento en dependencias críticas.
- **Acción:** Usar interceptores para reintentos exponenciales y colas de mensajes para eventos asíncronos.

### 4.3. Seguridad y Secretos
- **Secretos:** Llaves de Sellado Digital expuestas en BD.
- **Acción:** Cifrar campos sensibles en reposo usando Vault o KMS.

---
**Fin del Informe.**
